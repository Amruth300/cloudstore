spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${DB_NAME:cloudstore}
    username: ${DB_USERNAME:cloudstore}
    password: ${DB_PASSWORD:cloudstore}
  data:
    redis:
      host: redis
      port: 6379
  kafka:
    bootstrap-servers: kafka:9092
